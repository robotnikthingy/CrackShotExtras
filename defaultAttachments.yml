# Grenade:
  # SpawnMythicMobOnHit:
  # SpawnMythicMobOnShoot:
  # Shooting:
    # MythicMobSpawn:
      # Mob1:
        # Type:
        # Amount:
        # Radius:
      # Mob2:
        # Type:
        # Amount:
        # Radius:
  # Explosions:
    # CustomExplosion:
      # Explosion1:
        # Range:
          # Range1:
            # Radius:
            # Damage:
            # Ignite:
            # BlockDamage:
        # Particles: